# Password generator

Przykładowy szablon projektu dla generatora kodu. W katalogu `password_generator`
znajdują się źródła, a w `tests` testy. Testy odpalamy z poziomu głównego katalogu
projektu (czyli tego z `README.md`) za pomocą komendy `pytest`.
